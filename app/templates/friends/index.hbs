<h1>Friends Index</h1>
{{! The context here is the controller}}

<h3>Total friends: {{model.length}}</h3>

<ul>
  {{#each model as |friend|}}
    <li>
      {{link-to friend.fullName 'friends.show' friend class='class'}} <a href="#" {{action "delete" friend}}>&#x2716;</a>
    </li>
  {{/each}}
</ul>
